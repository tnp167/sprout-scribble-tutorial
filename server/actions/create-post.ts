"use server";

import { db } from "@/server";
import { posts } from "../schema";

export default async function creatPost(formData: FormData) {
  // const post = await db.insert(posts).values({
  //   title,
  // });
  // if (!posts) {
  //   return { error: "No posts found" };
  // }
  // return { success: posts };
}
